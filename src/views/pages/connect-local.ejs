<!doctype html>
<html>

<head>
    <% include ../partials/header %>
        <title>Chaddon | Link Guest Account</title>
        <link rel="stylesheet" type="text/css" href="css/connect-local.css">
</head>

<body>
    <main>
        <h1>
            <i class="fas fa-link"></i> Link Guest Account
        </h1>
        <div class="connect-local">
            <div class="container">
                <% if (message.length > 0) { %>
                    <div class="alert alert-danger">
                        <%= message %>
                    </div>
                    <% } %>
                        <form action="/connect/local" method="post">
                            <% include ../partials/formInputFields %>
                                <div class="row justify-content-center">
                                    <div class="col-4">
                                        <button type="submit" class="btn btn-success" id="loginBtn">Login</button>
                                    </div>
                        </form>
                        <div class="col-4">
                            <a href="/profile">
                                <button type="button" class="btn btn-outline-secondary" id="backToLoginBtn">Back</button>
                            </a>
                        </div>
                        </div>
                        <div>
    </main>
    <footer>
        <% include ../partials/footer %>
    </footer>
</body>

</html>